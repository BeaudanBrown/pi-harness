watch_file .envrc
watch_file flake.nix
watch_file flake.lock

while IFS= read -r -d '' path; do
  case "$path" in
    *.nix | *.ts | *.json | *.md | *.yaml | *.yml) watch_file "$path" ;;
  esac
done < <(
  git ls-files --cached --others --exclude-standard -z -- nix config .pi
)

use flake
