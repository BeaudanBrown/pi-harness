watch_file .envrc
watch_file flake.nix
watch_file flake.lock

while IFS= read -r path; do
  watch_file "$path"
done < <(
  find nix config .pi -type f \
    \( -name '*.nix' -o -name '*.ts' -o -name '*.json' -o -name '*.md' -o -name '*.yaml' -o -name '*.yml' \) \
    | sort
)

use flake
