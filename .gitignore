
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Private operator recovery backups must never enter Git or review snapshots.
/backup/

# Local developer/runtime artifacts
.direnv/
.loom/
.pi/
.pi-types/
.pi-test-build/
/result
/result-*
